/**
 * Interface for  Landing data
 * @interface
 * @property {string} scaling - text
 * @property {string} title - text
 * @property {string} landingImage - url
 */

export interface Landing {
    scaling: string;
    title: string;
    landingImage: string;
  }