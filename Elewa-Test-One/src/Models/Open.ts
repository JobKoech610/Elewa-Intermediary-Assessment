/**
 * Interface for  Landing data
 * @interface
 * @property {string} openImageCard - url
 * @property {string} openHead - text
 * @property {string} openText - text
 */

export interface Open {
    openImageCard: string;
    openHead: string;
    openText: string;
  }