/**
 * Interface for  Social data
 * @interface
 * @property {string} socialImageCard - image url
 * @property {string} socialHeading - text
 * @property {string} socialText - Text
 */

export interface Social {
    socialImageCard: string;
    socialHeading: string;
    socialText: string;
  }