/**
 * Interface for  beyond data
 * @interface
 * @property {string} beyondImageCard - image url
 * @property {string} beyondHead - text
 * @property {string} beyondText - Text
 */

export interface Beyond {
    beyondImageCard: string;
    beyondHead: string;
    beyondText: string;
  }